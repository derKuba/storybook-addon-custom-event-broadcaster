import { styled } from "@storybook/theming";

export const FlexedBox = styled.div({
  display: "flex",
});

export const IconBox = styled.div({
  width: "25px",
  height: "25px",
});

export const IconLabelBox = styled.div({
  marginLeft: "5px",
  marginTop: "3px",
});
